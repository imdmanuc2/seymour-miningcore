# API Reference

The dashboard consumes the MiningCore REST API.

Typical endpoints include:

    GET /api/pools
    GET /api/pools/{coin}
    GET /api/pools/{coin}/miners/{address}
    GET /api/pools/{coin}/miners/{address}/performance
    GET /api/blocks

Additional Seymour-specific endpoints may be added in future releases.
